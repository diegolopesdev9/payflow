TAREFA: Adicionar método updateUser na classe SupabaseStorage no arquivo supabase.ts.

⚠️ INSTRUÇÕES CRÍTICAS:
- MODIFIQUE APENAS server/supabase.ts
- Adicione o método updateUser na classe SupabaseStorage
- NÃO altere mais nada
- Mostre o diff antes de aplicar

ARQUIVO: server/supabase.ts

---

PROBLEMA:
O método updateUser retorna erro "The result contains 0 rows" porque está tentando usar .single() em uma query que não encontra o usuário, ou o método não existe corretamente na classe SupabaseStorage.

LOCALIZAR a classe SupabaseStorage no arquivo server/supabase.ts

PROCURAR pelo método getUserByEmail

ADICIONAR o método updateUser LOGO APÓS getUserByEmail:
```typescript
async updateUser(userId: string, data: { name?: string; email?: string }): Promise<User | null> {
  console.log('💾 [SupabaseStorage.updateUser] Atualizando usuário:', userId, data);
  
  try {
    const updateData: any = {};
    if (data.name !== undefined) updateData.name = data.name;
    if (data.email !== undefined) updateData.email = data.email;

    console.log('📝 [SupabaseStorage.updateUser] Dados a enviar:', updateData);

    const { data: user, error } = await this.supabase
      .from('users')
      .update(updateData)
      .eq('id', userId)
      .select()
      .single();

    if (error) {
      console.error('❌ [SupabaseStorage.updateUser] Erro do Supabase:', error);
      
      // Se não encontrou nenhuma linha, retornar null
      if (error.code === 'PGRST116') {
        console.log('⚠️ Usuário não existe no banco:', userId);
        return null;
      }
      
      throw new Error(error.message);
    }

    if (!user) {
      console.log('⚠️ Update não retornou usuário');
      return null;
    }

    console.log('✅ [SupabaseStorage.updateUser] Usuário atualizado:', user);
    
    // Converter snake_case para camelCase
    return {
      id: user.id,
      email: user.email,
      name: user.name || null,
      createdAt: user.created_at ? new Date(user.created_at) : new Date(),
    };
  } catch (error: any) {
    console.error('❌ [SupabaseStorage.updateUser] Erro:', error);
    throw error;
  }
}
```

---

ESTRUTURA CORRETA:
```typescript
export class SupabaseStorage implements IStorage {
  // ... outros métodos ...

  async getUserByEmail(email: string): Promise<User | null> {
    // ... código existente ...
  }

  // ✅ ADICIONAR AQUI
  async updateUser(userId: string, data: { name?: string; email?: string }): Promise<User | null> {
    // ... código acima ...
  }

  async createUser(userData: NewUser): Promise<User> {
    // ... código existente ...
  }

  // ... outros métodos ...
}
```

---

RESULTADO ESPERADO:
Método updateUser funciona corretamente no Supabase.

VALIDAÇÃO:
1. Aplique a mudança
2. Rebuild: pkill -f node && npm run build && npm run start
3. Teste salvar o perfil
4. Deve aparecer mensagem de sucesso

Mostre o diff e aplique.