TAREFA ETAPA 1/3: Criar componente BottomNavigation reutilizável e aplicar em todas as páginas (Dashboard, Bills, Profile, Reports).

INSTRUÇÕES CRÍTICAS:
- CRIAR arquivo src/components/BottomNavigation.tsx
- MODIFICAR src/pages/Dashboard.tsx (substituir navegação inline)
- MODIFICAR src/pages/Bills.tsx (substituir navegação inline)
- MODIFICAR src/pages/Profile.tsx (substituir navegação inline)
- NÃO modifique nenhum outro arquivo
- Mostre o diff de TODOS os arquivos modificados

PASSO 1: CRIAR novo arquivo src/components/BottomNavigation.tsx com este conteúdo:

import { useLocation } from "wouter";
import { Home, CreditCard, TrendingUp, User } from "lucide-react";

export function BottomNavigation() {
  const [location, setLocation] = useLocation();

  const navItems = [
    { path: "/dashboard", icon: Home, label: "Home" },
    { path: "/bills", icon: CreditCard, label: "Contas" },
    { path: "/reports", icon: TrendingUp, label: "Relatórios" },
    { path: "/profile", icon: User, label: "Perfil" },
  ];

  return (
    <div className="fixed bottom-0 left-0 right-0 bg-card/95 backdrop-blur-sm border-t border-border z-50">
      <div className="container mx-auto px-4">
        <div className="grid grid-cols-4 gap-1">
          {navItems.map(({ path, icon: Icon, label }) => (
            <button
              key={path}
              onClick={() => setLocation(path)}
              className={`nav-item ${location === path ? "active" : ""}`}
            >
              <Icon className="w-5 h-5" />
              <span className="text-sm">{label}</span>
            </button>
          ))}
        </div>
      </div>
    </div>
  );
}

PASSO 2: MODIFICAR src/pages/Dashboard.tsx
LOCALIZAR o bloco da navegação inferior (últimas ~20 linhas antes do final):
{/* Bottom Navigation */}
<div className="fixed bottom-0 left-0 right-0 bg-card/95 backdrop-blur-sm border-t border-border">
  <div className="container mx-auto px-4">
    <div className="grid grid-cols-4 gap-1">
      <Link href="/dashboard">
        ...
      </Link>
      ...
    </div>
  </div>
</div>

SUBSTITUIR POR:
<BottomNavigation />

E ADICIONAR no topo do arquivo:
import { BottomNavigation } from "@/components/BottomNavigation";

PASSO 3: MODIFICAR src/pages/Bills.tsx
FAZER a mesma substituição:
- Remover o bloco completo da navegação inferior
- Substituir por: <BottomNavigation />
- Adicionar import no topo: import { BottomNavigation } from "@/components/BottomNavigation";

PASSO 4: MODIFICAR src/pages/Profile.tsx
FAZER a mesma substituição:
- Remover o bloco completo da navegação inferior
- Substituir por: <BottomNavigation />
- Adicionar import no topo: import { BottomNavigation } from "@/components/BottomNavigation";

EXPLICAÇÃO:
- Cria componente reutilizável BottomNavigation
- Todas as páginas usam o mesmo componente
- Navegação sempre padronizada
- Se mudar em um lugar, muda em todos
- Código limpo e DRY (Don't Repeat Yourself)

NÃO MODIFIQUE MAIS NADA. Mostre o diff de TODOS os 4 arquivos (1 novo + 3 modificados) e aguarde confirmação.