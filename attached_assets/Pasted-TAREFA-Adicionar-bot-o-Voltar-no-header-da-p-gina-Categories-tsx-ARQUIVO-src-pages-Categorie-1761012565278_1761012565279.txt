TAREFA: Adicionar botão "Voltar" no header da página Categories.tsx.

ARQUIVO: src/pages/Categories.tsx

LOCALIZAR (aproximadamente linha 315):
      <div className="bg-primary/80 backdrop-blur-sm border-b border-primary-foreground/10">
        <div className="container mx-auto px-4 py-6">
          <div className="flex items-center justify-between">
            <div>
              <h1 className="text-2xl font-bold text-primary-foreground">Categorias</h1>
              <p className="text-primary-foreground/80">Organize suas despesas</p>
            </div>
            <Button onClick={openCreateDialog} className="btn-financial">
              <Plus className="w-4 h-4 mr-2" />
              Nova Categoria
            </Button>
          </div>
        </div>
      </div>

SUBSTITUIR POR:
      <div className="bg-primary/80 backdrop-blur-sm border-b border-primary-foreground/10">
        <div className="container mx-auto px-4 py-6">
          <div className="flex items-center gap-4">
            <Button
              variant="ghost"
              size="sm"
              onClick={() => setLocation("/dashboard")}
              className="text-primary-foreground hover:bg-primary-foreground/10"
            >
              <ArrowLeft className="w-4 h-4" />
            </Button>
            <div className="flex-1 flex items-center justify-between">
              <div>
                <h1 className="text-2xl font-bold text-primary-foreground">Categorias</h1>
                <p className="text-primary-foreground/80">Organize suas despesas</p>
              </div>
              <Button onClick={openCreateDialog} className="btn-financial">
                <Plus className="w-4 h-4 mr-2" />
                Nova Categoria
              </Button>
            </div>
          </div>
        </div>
      </div>

EXPLICAÇÃO:
- Adiciona botão de voltar com ícone de seta à esquerda
- Volta para /dashboard ao clicar
- Usa mesmo estilo dos outros headers do app
- Mantém layout responsivo e bonito

VALIDAÇÃO:
Após aplicar, deve aparecer botão de seta à esquerda do título "Categorias" que volta para o dashboard.

IMPORTANTE: Certifique-se que ArrowLeft já está importado no topo do arquivo (deve estar na linha dos imports do lucide-react).

NÃO MODIFIQUE MAIS NADA. Mostre o diff completo e aplique.