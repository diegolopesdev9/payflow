
# ===================================
# PAYFLOW - Environment Variables
# ===================================
# IMPORTANTE: No Replit, configure estes valores em "Secrets" (🔒 ícone de cadeado)
# Nunca commite o arquivo .env real com valores reais!

# --- API Configuration ---
PORT_API=8080
NODE_ENV=development

# --- Supabase (Database & Auth) ---
# Obtenha em: https://app.supabase.com/project/_/settings/api
SUPABASE_URL=https://seu-projeto.supabase.co
SUPABASE_ANON_KEY=sua-chave-anonima-aqui
SUPABASE_SERVICE_ROLE_KEY=sua-service-role-key-aqui

# --- Database (Prisma) ---
# Formato: postgresql://user:password@host:port/database?schema=public
DATABASE_URL=postgresql://postgres:password@db.xxx.supabase.co:6543/postgres?pgbouncer=true
DIRECT_URL=postgresql://postgres:password@db.xxx.supabase.co:5432/postgres

# --- Frontend (Vite) ---
VITE_API_URL=http://localhost:8080
VITE_SUPABASE_URL=${SUPABASE_URL}
VITE_SUPABASE_ANON_KEY=${SUPABASE_ANON_KEY}

# --- Rate Limiting ---
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# --- CORS ---
CORS_ORIGIN=*
# Produção: CORS_ORIGIN=https://replit.com/@diegolopesdev9/PayFlow
